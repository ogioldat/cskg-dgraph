query find_predecessors($id: string, $first: int = 10000, $offset: int = 0) {
  q(func: uid($id)) {
    uid
    label
    # ~rel denotes the reverse edge (incoming connections)
    ~rel @facets {
      uid
      label
    }
  }
}
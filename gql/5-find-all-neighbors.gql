query find_neighbors($id: string, $first: int = 10000, $offset: int = 0) {
  q(func: uid($id)) {
    uid
    label
    # Outgoing neighbors
    rel @facets {
      uid
      label
    }
    # Incoming neighbors
    ~rel @facets {
      uid
      label
    }
  }
}